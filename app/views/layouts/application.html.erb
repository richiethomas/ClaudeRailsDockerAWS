<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <!-- Navigation Bar -->
    <nav class="bg-gray-800 text-white px-4 py-3">
      <div class="container mx-auto flex justify-between items-center">
        <div class="flex gap-4">
          <%= link_to "Blog", root_path, class: "text-xl font-bold hover:text-gray-300" %>
          <%= link_to "Posts", posts_path, class: "hover:text-gray-300" %>
          <% if user_signed_in? %>
            <%= link_to "Admin", admin.root_path, class: "hover:text-gray-300" %>
          <% end %>
        </div>

        <div class="flex gap-4 items-center">
          <% if user_signed_in? %>
            <span class="text-gray-300">Logged in as <%= current_user.email %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-red-600 px-3 py-1 rounded hover:bg-red-700" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "bg-blue-600 px-3 py-1 rounded hover:bg-blue-700" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "bg-green-600 px-3 py-1 rounded hover:bg-green-700" %>
          <% end %>
        </div>
      </div>
    </nav>
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4 mx-auto max-w-4xl mt-4">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 mx-auto max-w-4xl mt-4">
        <%= alert %>
      </div>
    <% end %>

    <main class="container mx-auto mt-8 px-5">
      <%= yield %>
    </main>
  </body>
</html>
